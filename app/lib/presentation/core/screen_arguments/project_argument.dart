import 'package:vision_director/domain/projects/project.dart';

/// Type-safe route argument to provide a [project].
class ProjectArgument {
  final Project project;
  ProjectArgument(this.project);
}
